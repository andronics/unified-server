{
  "query": "mutation Register($input: RegisterInput!) { register(input: $input) { user { id } } }",
  "variables": {
    "input": {
      "email": "weak@example.com",
      "name": "Weak",
      "password": "weak"
    }
  }
}
